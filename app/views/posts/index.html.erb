<%= link_to 'Post a pet', new_post_path %>
<%= link_to "Search a location", places_path %>

<h1>Featured Pets</h1>

<div class="container">
    <% @posts.each do |post| %>
        <div class="card" style="width: 15rem;">
            <%= image_tag post.thumbnail_url, class: "card-img-top" %>
            <div class="card-body">
                <h3 class="card-title"><%= post.pet_name %></h3>
                <p class="card-text"><%= post.category %>, <%= post.breed %></p>
                <p class="card-text"><%= post.description %></p>
                <%= link_to "Details", post_path(post), class: "btn btn-secondary btn-sm" %>
            </div>
        </div>
    <% end %>
</div>


